#include <iostream>
#include <string>
using namespace std;

bool find(string &word)
{
    return false;
}

int main()
{
    string s;
    while (string::iterator iter != s.end()) { /* ... */ }
    string word;
    while (bool status = find(word)) { /* ... */ }
        if (!status) { /* ... */ }
    return 0;
}