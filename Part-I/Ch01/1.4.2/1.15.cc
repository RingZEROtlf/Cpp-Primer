#include <iostream>

int main()
{
    // syntax error
    std::cout << "Read each file." << std::endl:
    std::cout << Update master. << std::endl;
    std::cout << "Write new master." std::endl;

    // type error
    int value;
    value = "hello";

    // declaration error
    int v1 = 0, v2 = 0;
    std::cin >> v >> v2;
    cout << v1 + v2 << std::endl;

    return 0;
}